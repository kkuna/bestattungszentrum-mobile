# AGENTS.md

<!-- Add your custom instructions below. Repowise will never modify anything outside the REPOWISE markers. -->
<!-- Examples: coding style rules, test commands, workflow preferences, constraints -->

## Verification

- Use Argent simulator/emulator verification for every change that affects app runtime behavior, navigation, screens, UI, device integration, storage/session flow, or API behavior that can be exercised through the app.
- If a change is pure non-UI logic and cannot be meaningfully exercised through Argent, state that explicitly in the final response and list the substitute checks that were run.
- Do not claim simulator/emulator verification unless Argent was actually used in the current workflow.

## Backend Gaps

- When implementation, review, or Argent verification reveals a backend/API contract gap, document it in `_bmad-output/implementation-artifacts/backend-gaps.md` before closing the task.
- Each backend gap entry should include status, source story/review, impacted mobile behavior, current workaround or verification limit, and the backend action needed.
- If a backend gap is fixed during the task, update the entry as resolved and keep the historical note rather than deleting it.

## BMAD Method

This project uses the BMAD Method (v6.7.1). Its skills are installed as self-contained skill packages under `.agents/skills/`, each named `bmad-*` with a `SKILL.md` entry file.

- To see what BMAD can do and route to the right workflow, load `.agents/skills/bmad-help/SKILL.md` and follow it (this is the `bmad-help` skill).
- Before performing a BMAD task (PRD, architecture, story creation, dev-story, code review, brainstorming, research, etc.), read the matching `.agents/skills/bmad-<name>/SKILL.md` and follow its instructions exactly.
- Outputs go to `_bmad-output/` (planning artifacts, implementation artifacts) per `_bmad/bmm/config.yaml`.
- Config lives in `_bmad/config.toml` and `_bmad/*/config.yaml`; treat installer-managed files as read-only and put durable overrides in `_bmad/custom/`.

<!-- REPOWISE:START — Do not edit below this line. Auto-generated by Repowise. -->
## IMPORTANT: Codebase Intelligence Instructions for Bestattungszentrum

> This repository is indexed by [Repowise](https://repowise.dev).
> Use the MCP tools below for orientation, discovery, and enriched context
> (documentation, ownership, history, decisions). **Always verify against
> actual source files before making changes** — the index may be stale.

Last indexed: 2026-05-21 (commit 386d806). Confidence: 100%.
### Architecture
This repository is a React Native mobile application built with Expo: it ingests configuration from app.json and TypeScript entry points, transpiles and bundles source code through Metro and Babel, applies theme and i18n middleware, and outputs a runnable iOS/Android app with navigation, API integration, and persistent storage. **Core Framework & Runtime**
- **React Native** (cpp) — cross-platform mobile UI framework
- **Expo** (typescript) — managed React Native platform with dev tools and native modules
- **React** (javascript) — UI component library
- **React Router** via **expo-router** (javascript) — file-based navigation

**Language & Build**
- **TypeScript** (81% of codebase) — static type checking
- **Babel** — JavaScript transpilation with React/JSX support
- **Metro** — React Native bundler and transformer
- **esbuild** — fast JavaScript bundler
- **tsx** — TypeScript execution runtime

**State & Storage**
- **react-native-mmkv** (cpp) — high-performance key-value storage
- **Reactotron** (typescript) — debugging and state inspection
- **Zustand** (implicit via reactotron integration) — state management

**Styling & Theme**
- **react-native-reanimated** (typescript) — performant animations
- **react-native-gesture-handler** (typescript) — gesture recognition
- **inline-style-prefixer** — CSS vendor prefixing
- **lightningcss** — CSS processing

**Internationalization**
- **i18next** (javascript) — translation framework
- **react-i18next** (javascript) — React bindings for i18n

**API & Networking**
- **axios** (javascript) — HTTP client
- **apisauce** (javascript) — axios wrapper with standardized responses
- **cross-fetch** — polyfill for fetch API

**Development & Testing**
- **Jest** — unit testing framework with Expo preset
- **ESLint** — code linting with React/React Native plugins
- **Prettier** — code formatting
- **TypeScript** — type checking

**Native Modules**
- **expo-constants** — app metadata and constants
- **expo-file-system** — file I/O
- **expo-font** — custom font loading
- **expo-image** — optimized image rendering
- **expo-linking** — deep linking
- **expo-localization** — device locale detection
- **expo-splash-screen** — splash screen management
- **expo-keep-awake** — prevent screen sleep



| File | Purpose |
|------|---------|
| app.json | Expo app configuration (name, version, plugins, permissions) |
| src/app/index.tsx | Root application component and navigation setup |
| src/config/index.ts | Centralized configuration (API endpoints, feature flags) |
| src/i18n/index.ts | i18next initialization and language setup |
| src/services/api/index.ts | API client factory and request/response interceptors |
| src/utils/storage/index.ts | MMKV storage abstraction layer |

**Build Entry:** Metro reads app.json and bundles from src/app/index.tsx → outputs .ios.js and .android.js bundles. **Theme System**
- src/theme/context.tsx provides a React Context for theme switching
- src/theme/types.ts defines theme shape (colors, spacing, typography)
- Components consume theme via useContext(ThemeContext)
- Highest PageRank files: en.ts (translations), types.ts (theme interface), context.tsx (provider)

**Internationalization**
- src/i18n/index.ts initializes i18next with language detection
- Translation files (e.g., en.ts) are keyed JSON objects
- Components use useTranslation() hook from react-i18next

**API Layer**
- src/services/api/index.ts exports a configured axios instance
- apisauce wraps axios for consistent error handling
- Interceptors inject auth tokens and handle 401/403 responses

**Storage**
- src/utils/storage/index.ts abstracts react-native-mmkv
- Provides get(), set(), remove() methods
- Synchronous API (unlike AsyncStorage)

**Navigation**
- expo-router provides file-based routing (similar to Next.js)
- Routes defined by file structure in src/app/
- Deep linking configured in app.json



**Most Changed (90 days):**
- .dependency-cruiser.js — dependency validation rules
- .eslintrc.js — linting configuration
- app.config.ts — dynamic Expo config

**Hotspots:** 18 files flagged as high-churn AND high-complexity (likely screens and API integration points). **Metro Bundler** reads app.json and tsconfig.json
2.
### Key Modules
| Module | Purpose | Owner |
|--------|---------|-------|
| `community-0` | The **components module** is the presentation layer and UI orchestration subsyst | — |
| `community-4` | The **theme module** is the styling and design-token persistence layer for repow | — |
### Entry Points
- `src/i18n/index.ts`
- `src/utils/storage/index.ts`
- `src/config/index.ts`
- `app.json`
- `src/app/index.tsx`
- `src/services/api/index.ts`
### Tech Stack
**Languages:** Node.js, TypeScript
**Frameworks:** React

### Hotspots (High Churn)
| File | Churn | 90d Commits | Owner |
|------|-------|-------------|-------|
| `src/components/Header.tsx` | 98.6th %ile | 1 | kkuna |
| `src/components/Card.tsx` | 97.3th %ile | 1 | kkuna |
| `src/components/Screen.tsx` | 95.9th %ile | 1 | kkuna |
| `src/components/TextField.tsx` | 94.5th %ile | 1 | kkuna |
| `src/components/Toggle/Toggle.tsx` | 93.2th %ile | 1 | kkuna |

## Code health
Hotspot health: 9.08/10 (stable) ·
Average: 9.41/10 ·
Worst: 8.0/10 (`src/theme/context.tsx`)

### Critical biomarkers
- `src/theme/context.tsx` — untested hotspot — impact −2.0

### Repowise MCP Tools

This repo has the Repowise MCP server configured. The tools below answer questions `grep`/`Read` cannot. Every response carries an `_meta` envelope with `index_age_days`, `indexed_commit`, and a `stale_warning` only when the index has actually diverged from HEAD — silence means the index is current.

**When to call which tool:**

| Tool | What only this tool answers |
|------|------------------------------|
| `get_answer(question)` | Synthesised answer with verified citations and a calibrated `retrieval_quality`. First call for "how does X work" / "why is Y like this". On low confidence returns `best_guesses` with one-line justifications instead of an empty answer. |
| `get_context(targets=[...])` | Triage card for files/modules/symbols — title, summary, signatures, `hotspot` bit, `decision_records` titles, and `symbol_id`s to pipe into `get_symbol`. Use `include=["callers","ownership",...]` to widen. NOT for source bytes. |
| `get_symbol("path/to/file.py::Name")` | Raw source bytes for one indexed symbol with exact line bounds. Cheaper and safer than `Read` + offset math. Use the `symbol_id` returned by `get_context`. |
| `search_codebase(query, kind?)` | Find pages by concept when you don't know the file. Each result carries `search_method` (`embedding` vs `bm25` fallback). For exact identifiers use Grep — the tool will hint when it sees one. |
| `get_why(query, targets?)` | Architectural decision archaeology — *why* the code is shaped this way. Call before refactors or pattern divergences. Falls back to git archaeology when no ADRs exist for a file. |
| `get_risk(targets, changed_files?)` | What history says about touching these files: churn, owners, blast radius. Pass `changed_files` for PR mode → returns a `directive` (`will_break`, `missing_cochanges`, `missing_tests`). |
| `get_dead_code(...)` | Tiered unreachable / unused-export / zombie-package findings. Run before a cleanup sprint, not before a targeted fix. |
| `get_overview(repo?)` | Architecture map for an unfamiliar repo. One-time orientation; skip on subsequent calls in the same session. |

**Composition tips:**
- `get_answer` → if `confidence` is `medium`/`low`, follow the `best_guesses[0].file` or `fallback_targets[0]` into `get_context`, then `get_symbol` for bytes.
- `get_context` returns `decision_records` titles → call `get_why(targets=[...])` for the rationale.
- `get_context` returns `hotspot: true` → call `get_risk` before editing.
- PR review → `get_risk(targets=[...], changed_files=[...])`; read the `directive` block first.

**Verify when:** `_meta.stale_warning` is present, or `retrieval_quality` is `partial`/`weak`, or `search_method` is `bm25`. Otherwise trust the response and act on it.

### Codebase Conventions
**Commands:**
- Test: `pnpm test`
- Lint: `pnpm lint`
- Dev: `pnpm start`

<!-- REPOWISE:END -->
